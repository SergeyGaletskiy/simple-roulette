import styled from 'styled-components';

export const PlayerBetsCardHeaderStyled = styled.div`
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  border-bottom: 1px solid var(--color-gray-30);
  margin-bottom: 15px;
  padding-bottom: 10px;
`;
