import styled from 'styled-components';

export const PlayerBetsCardStyled = styled.div`
  background-color: var(--bg-main);
  box-shadow: var(--shadow-main);
  border-radius: 15px;
  padding: 15px;
`;
